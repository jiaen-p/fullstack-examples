import { TemperaturaConverterPipe } from './temperatura-converter.pipe';

describe('TemperaturaConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new TemperaturaConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
